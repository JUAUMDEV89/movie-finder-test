import styled  from 'styled-components';

export const Header = styled.header`
   width: 100%;
   display:flex;
   align-items: center;
   padding-left: 5rem;
   background: transparent;
   height: 5rem;
   position: fixed;

   img{
      width: 8rem;
      height: 4rem;
   }
`;